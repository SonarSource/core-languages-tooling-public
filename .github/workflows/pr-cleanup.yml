name: Cleanup PR Resources
on:
  pull_request:
    types: [closed]

jobs:
  cleanup:
    # Runner selection based on repository visibility:
    # - Private/internal repositories: sonar-xs
    # - Public repositories: github-ubuntu-latest-s
    runs-on: github-ubuntu-latest-s
    name: Cleanup
    permissions:
      actions: write
    steps:
      - uses: SonarSource/ci-github-actions/pr_cleanup@9d3488f52305895f599feb1ec8560c1a5339aa99 # 1.8.1
